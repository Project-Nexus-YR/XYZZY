# Trace

Every claim on site/index.html, with what proves it. Commands were re-run on
2026-09-04 from the repo root, against the tree after the services split.

| Claim on the page | Proof | Evidence |
|---|---|---|
| "Conversation branches into parallel specialists" | `src/multiplayer/services/branches.py:73` (`start_branch`) / `src/multiplayer/services/branches.py:93` (agent-count check) / `src/multiplayer/services/runs.py:218` (`execute_branch_run`) / test | `start_branch` (branches.py:72) requires `2 <= len(agent_ids) <= 3` for `BranchMode.PARALLEL` (branches.py:93: `raise DomainError("parallel mode requires two or three agents")`). `execute_branch_run` (runs.py:218) runs one agent's step. That more than one agent actually runs and both outputs are recorded within one branch is asserted by `tests/e2e/test_first_class_branches.py:168` `test_model_synthesis_uses_selected_branch_outputs_and_links_artifact_version`, which starts a branch with 3 agents, calls `execute_branch_run` once per run, and asserts `len(await service.repos.agent_outputs.list_by_branch(branch.branch_id)) == 3` (`tests/e2e/test_first_class_branches.py:217`). |
| "Each output is explicitly included or excluded before synthesis" | `src/multiplayer/services/branches.py:355` (`select_branch_output`) / `src/multiplayer/services/branches.py:433` (refusal) / test (positive path only) | `select_branch_output` (branches.py:354) records an `OutputDisposition` per output. `synthesize_branch` (branches.py:401) computes the undecided set and at branches.py:433 `raise DomainError("every branch output must be included or excluded")` if any output lacks a disposition. The positive path (every output gets a disposition, INCLUDED or EXCLUDED, before synthesis succeeds) is asserted by `tests/e2e/test_first_class_branches.py:168` (dispositions set at `tests/e2e/test_first_class_branches.py:196`, synthesis then succeeds at `tests/e2e/test_first_class_branches.py:198`). UNPROVEN for the refusal itself: searched tests/ for a test that calls `synthesize_branch`/`synthesize_branch_decision_brief` with an output left undecided and asserts the "every branch output must be included or excluded" DomainError; no such test found (`grep -rn "must be included or excluded" tests/` returns nothing). |
| "XYZZY synthesizes a decision brief with the evidence traced beneath it" / "Each Decision also links to the Claims and AgentOutputs behind it" | `src/multiplayer/domain/models.py:992` (`ArtifactClaim`) / `src/multiplayer/domain/models.py:1004` (`ClaimSource`) / `src/multiplayer/migrations/003_frozen_artifact_provenance.sql` / test | `ArtifactClaim` (models.py:992, fields incl. `version_id`, `text`) and `ClaimSource` (models.py:1004, fields incl. `claim_id`, `output_id`, `evidence`) chain a synthesized Decision's claims back to the AgentOutput that produced them; `artifact_claims`/`claim_sources` tables are defined in migrations/003_frozen_artifact_provenance.sql. Traversability is asserted by `tests/e2e/test_first_class_branches.py:214`: `claims = await service.repos.artifacts.get_version_provenance(version.version_id)`, then `tests/e2e/test_first_class_branches.py:215`: `assert {claim["output_id"] for claim in claims} == set(output_ids[:2])`. |
| "Actions wait for human approval before they execute" | `src/multiplayer/security/capabilities.py:233` (`ToolSpec`, `requires_approval` field) / `src/multiplayer/security/capabilities.py:249-250` (`task.create`, `artifact.write`) / test | `ToolSpec` (capabilities.py:233) has a `requires_approval` field; `TOOLS` marks `task.create` and `artifact.write` `requires_approval=True` at capabilities.py:249 and capabilities.py:250. Blocked-then-proceeds is asserted by `tests/security/test_model_tool_gateway.py:240` `test_a_tool_that_requires_approval_stops_at_the_reviewer`: after the model calls `artifact.write`, `result["tool_request"]["status"] == "PENDING_APPROVAL"` (`tests/security/test_model_tool_gateway.py:252`) and `await svc.list_room_artifacts(room_id) == []` (`tests/security/test_model_tool_gateway.py:254`), then after approval, `artifacts = await svc.list_room_artifacts(room_id)` is non-empty (`tests/security/test_model_tool_gateway.py:260`). |
| "What an agent may do is re-read from the room's own state at the moment it acts, so leaving a room or losing access takes effect immediately, mid-task" | `src/multiplayer/services/steps.py:259` and `src/multiplayer/services/steps.py:262` (re-derives effective capabilities inside the step path, not cached from run start) / test | Inside the step path (steps.py), `authorization = await self._authorization_for(...)` and `effective = (await self._authorized_terms(authorization)).effective` are recomputed on every call, i.e. every step/tool call re-reads room membership/capabilities rather than reusing a value cached when the run started. Mid-task revocation taking effect on the very next action is asserted by `tests/security/test_continuation_authority.py:91` `test_a_grant_withdrawn_between_two_tool_calls_stops_the_second`: it revokes the caller's capabilities between two tool calls inside one turn, then asserts the second call never reached the model (`len(provider.prompts) == 1`, `tests/security/test_continuation_authority.py:125`), the run settled with `"no effective capability" in execution.error` (`tests/security/test_continuation_authority.py:136`), and `run["settlement"] == RunSettlement.AUTHORITY_REVOKED.value` (`tests/security/test_continuation_authority.py:140`). |
| "Every room's event log is hash-chained... tamper-evident by construction, checkable with the audit CLI" | `src/multiplayer/migrations/033_the_log_commits_to_its_past.sql` / `src/multiplayer/security/audit.py:111` (`event_chain_hash`) / `src/multiplayer/security/audit.py:154` (`verify_event_chain`) / `src/multiplayer/manage.py:12` (docstring) / `src/multiplayer/manage.py:132` (subcommand wired) / test | Migration 033 adds `room_events.prev_hash` and `event_hash` columns, chaining each event to the one before it. `event_chain_hash` (audit.py:111) computes the digest; `verify_event_chain` (audit.py:154) walks the chain. `manage.py` docstring documents `python -m multiplayer.manage app.db audit verify` (manage.py:12); the `audit verify` branch is wired at manage.py:132 (subparser at `src/multiplayer/manage.py:239-171`). Tamper detection is asserted by `tests/security/test_event_chain.py:83` `test_an_edited_payload_breaks_the_chain_at_its_row`, which edits a stored payload then asserts `verify_event_chain` reports a break at that row. |
| "One Python process and a SQLite file, self-hosted" | `Dockerfile:30` (`CMD ["python", "-m", "multiplayer.server", "/data/multiplayer.db"]`, one process) / `src/multiplayer/server.py:697` (`def main`) / `src/multiplayer/db/connection.py:1` / `src/multiplayer/db/connection.py:13` / `src/multiplayer/db/connection.py:45` | server.py:697 `def main()` builds one FastAPI app (`create_app`) and runs it under `uvicorn` in a single process: no separate worker/broker process is started. db/connection.py:1 docstring: "Database connection and query helpers using aiosqlite."; db/connection.py:13 `import aiosqlite`; db/connection.py:45 `self._db = await aiosqlite.connect(self._path, isolation_level=None)`, a single SQLite file, no external database service. |
| "Point specialists at Ollama, LM Studio, or any OpenAI-compatible server instead of a hosted API" | `src/multiplayer/model_providers/openai_responses.py:259` | `model_provider_from_environment` reads `base_url = values.get("XYZZY_LOCAL_MODEL_BASE_URL", "").strip()`; when set, it builds a chat-completions provider against that base URL, i.e. any base URL implementing the OpenAI chat-completions API, which Ollama and LM Studio both expose. `docker-compose.yml:15` shows a commented example: `XYZZY_LOCAL_MODEL_BASE_URL: "http://host.docker.internal:11434/v1"` (Ollama's default port). |
| "Apache-2.0 licensed" | `LICENSE:2` / `pyproject.toml:11` | `LICENSE` line 2: "Apache License", line 3: "Version 2.0, January 2004". `pyproject.toml:11`: `license = { text = "Apache-2.0" }`. |
| "1283 tests" | command run this session | `PYTHONPATH=src .venv/Scripts/python.exe -m pytest --collect-only -o addopts="" -q` was run from the repo root; last line: `1283 tests collected`. The page shows 1283, the count collected this session (re-measured after the round-2 critic pass); the previous page said 1063. |
| "Ruff and strict mypy in CI" | `pyproject.toml:73-76` / `.github/workflows/ci.yml:21-23` | `pyproject.toml:73-76`: `[tool.mypy]` / `python_version = "3.11"` / `strict = true` / `files = ["src"]`. `.github/workflows/ci.yml` lines 21-23: `- run: ruff check .`, `- run: ruff format --check .`, `- run: mypy src`. |
| `docker run -p 8000:8000 -e XYZZY_DEMO=1 ghcr.io/project-nexus-yr/xyzzy` opens a seeded workspace at localhost:8000 with one-click sign-in | `Dockerfile:23` / `Dockerfile:28` / `src/multiplayer/server.py:126` / `src/multiplayer/server.py:424` / `src/multiplayer/server.py:472` / `.github/workflows/docker-publish.yml:1` | `Dockerfile:23` `EXPOSE 8000`; `Dockerfile:28` `CMD ["python", "-m", "multiplayer.server", "/data/multiplayer.db"]`. server.py:126 `_demo_mode_requested()` reads `XYZZY_DEMO` from the environment. server.py:424 `auth_tokens = {DEMO_BEARER_TOKEN: DEMO_USER_ID}` (a fixed bearer token, the "one click" sign-in) and server.py:472 `await svc.seed_demo_workspace()` seeds the demo workspace. `.github/workflows/docker-publish.yml:1` (`name: image`) is the workflow that publishes that image only once the gates workflow has run green on main. The `docker run` command itself was not run this session (task forbids running docker). |
| `docker compose run --rm --service-ports demo` exists | `docker-compose.yml:27` | The `demo` service is defined with `XYZZY_DEMO: "1"`; the invocation is `run --rm --service-ports` rather than a profile-gated `up`, to avoid a port clash with the `xyzzy` service, which is profile-free. Not run this session (docker forbidden by task). |
| "Without an API key, runs produce a labelled SIMULATED output" (implied by the "self-hosted" / no-hosted-API framing on the page; the actual fallback behaviour) | `src/multiplayer/model_providers/openai_responses.py:33` / `src/multiplayer/model_providers/openai_responses.py:259` / test | openai_responses.py:259 and the lines after it: `api_key = values.get("OPENAI_API_KEY", "").strip()` then, when empty, a workflow-only provider is returned. openai_responses.py:33: the fallback content string starts `"SIMULATED WORKFLOW OUTPUT — no model provider is configured. "`. Asserted by `tests/regression/test_agent_outputs.py:60`: `assert output.content.startswith("SIMULATED WORKFLOW OUTPUT")`, and by `tests/security/test_agent_template_ownership.py:208` (same assertion). |
| "xyzzy 0.4.0" in the footer and "v0.4.0" in the hero proof line | `pyproject.toml:7` | `pyproject.toml:7`: `version = "0.4.0"`. The page footer reads `xyzzy 0.4.0 · Apache-2.0` and the hero proof line `v0.4.0`, both from that manifest line. |
| SECURITY.md exists (footer link "Security policy") | `SECURITY.md:1` / `SECURITY.md:3` | `SECURITY.md:1`: `# Security Policy`; `SECURITY.md:3`: `## Supported Versions`. |
| "Only what a person included reaches the brief" and the legend "excluded, kept in the log, absent from the brief" | `tests/e2e/test_first_class_branches.py:196` / `tests/e2e/test_first_class_branches.py:215` / `tests/e2e/test_first_class_branches.py:217` | Three outputs are recorded (`len(...list_by_branch(...)) == 3`, line 217), two are INCLUDED and one EXCLUDED (line 196), and the published version's provenance contains exactly the two included output ids (`== set(output_ids[:2])`, line 215). The excluded output stays in `agent_outputs` and is absent from the claims. |
| "The seeded demo workspace, as the one command below opens it" (hero screenshot caption) | `scripts/capture_hero.py` / `src/multiplayer/server.py:472` | The `site/assets/screenshot-hero-*.png` files were captured by `scripts/capture_hero.py` against the demo server (`XYZZY_DEMO=1 XYZZY_PORT=8010 python -m multiplayer.server`), whose seed is `seed_demo_workspace()` at server.py:472; the docker command runs the same server with the same flag. |
| "Two or three specialist agents run at once on the same question" (loop step 02) | `src/multiplayer/services/branches.py:73` / `src/multiplayer/services/branches.py:93` | `start_branch` requires `2 <= len(agent_ids) <= 3` for `BranchMode.PARALLEL`, else `DomainError("parallel mode requires two or three agents")` (branches.py:93); three run in `tests/e2e/test_first_class_branches.py:168`. |
| "Nothing is synthesized until every output has one" (loop step 03) | `src/multiplayer/services/branches.py:402` / `src/multiplayer/services/branches.py:433` / positive path tested | `synthesize_branch` raises `DomainError("every branch output must be included or excluded")` at branches.py:433 when any output lacks a disposition; the positive path is `tests/e2e/test_first_class_branches.py:196` through `tests/e2e/test_first_class_branches.py:198`. The refusal itself has no asserting test (see the row above). |
| The floating branch card and its tag "cropped from the capture above" | `scripts/capture_hero.py` (`crop_branch_card`) | `site/assets/branch-card-{light,dark}.png` and `@2x` are cropped by the capture script from `screenshot-hero-{theme}@2x.png` at `BRANCH_CARD_BOX_2X`, so the card is the same pixels as the workspace frame behind it. |
| The three tabbed views: "General, the shared room", "the branch, two specialists, a verdict each", "the Decision Brief, every claim with its source id" | `scripts/capture_hero.py` (`capture_views`) / `src/multiplayer/server.py:472` | `view-branch-*.png` and `view-brief-*.png` are captured by the producer from the seeded demo (`seed_demo_workspace()`, server.py:472) by clicking `button.branch-nav` and `#nav-artifacts`; the branch view shows Include and Exclude per output and "2 included · 0 excluded · 0 unreviewed" (see the capture), the brief shows two claims each with a `Sources: out_…` id (see the capture). |
| "No API key was set, so the specialist text reads SIMULATED" (stage caption) | `src/multiplayer/model_providers/openai_responses.py:33` / `src/multiplayer/model_providers/openai_responses.py:259` | Same evidence as the SIMULATED row above; the captured views show the literal SIMULATED text. |

## Slop pass

The one move a competitor could not copy without looking like a clone: **the stage**, three real captures of one seeded room (channel, branch view, Decision Brief) as tabs over a perspective stack on the deep green band, the chosen view in front and the others receding, with the room's own branch card floating in front of it; and below it the loop that draws itself a stage per step scrolled. The page's motion is the mechanism and its layers are the product's own screens. Colour: paper and deep forest green in alternating bands, gold for the one action, the tabs and the one italic word; green is the brand and the included mark, red the excluded mark. Kept by the user's call on 2026-09-02: the palette and the Cormorant display with the italic gold word, which the slop tells list as a reflex and which is here a decision. Reflexes removed: the gold full stop in the wordmark, the three-column feature grid (now a ledger with proof), the dash as sentence punctuation, a test count carried over from an earlier run, any gradient beyond one gold light on the stage, ambient looping motion, parallax on decorative shapes, and any screen that does not exist. Kept on purpose: the clone URL wrapping at 390 px rather than hiding behind a scrollbar, and the SIMULATED text visible in the captures rather than a retouched one.
