# Trace

Every claim on site/index.html, with what proves it. Commands were run on 2026-09-02 from the repo root.

| Claim on the page | Proof | Evidence |
|---|---|---|
| "Conversation branches into parallel specialists" | src/multiplayer/services/service.py:2527, 2548 / src/multiplayer/services/service.py:3896 / test | `start_branch` (service.py:2527) requires `2 <= len(agent_ids) <= 3` for `BranchMode.PARALLEL` (service.py:2548: `raise DomainError("parallel mode requires two or three agents")`). `execute_branch_run` (service.py:3896) runs one agent's step. That more than one agent actually runs and both outputs are recorded within one branch is asserted by `tests/e2e/test_first_class_branches.py:168` `test_model_synthesis_uses_selected_branch_outputs_and_links_artifact_version`, which starts a branch with 3 agents, calls `execute_branch_run` once per run (loop at line 183-185), and asserts `len(await service.repos.agent_outputs.list_by_branch(branch.branch_id)) == 3`. |
| "Each output is explicitly included or excluded before synthesis" | src/multiplayer/services/service.py:4193 (`select_branch_output`) / :4271 (refusal) / test (positive path only) | `select_branch_output` (service.py:4193) records an `OutputDisposition` per output. `synthesize_branch` (service.py:4240) computes `unreviewed = [output.output_id for output in outputs if output.output_id not in decisions]` and at line 4271 `raise DomainError("every branch output must be included or excluded")` if any output lacks a disposition. The positive path (every output gets a disposition, INCLUDED or EXCLUDED, before synthesis succeeds) is asserted by `tests/e2e/test_first_class_branches.py:168` (dispositions set at lines 191-196, synthesis then succeeds at line 198). UNPROVEN for the refusal itself: searched tests/ for a test that calls `synthesize_branch`/`synthesize_branch_decision_brief`/`synthesize_decision_brief` with an output left undecided and asserts the "every branch output must be included or excluded" DomainError; no such test found (`grep -rn "must be included or excluded" tests/` returned nothing). |
| "XYZZY synthesizes a decision brief with the evidence traced beneath it" / "Each Decision also links to the Claims and AgentOutputs behind it" | src/multiplayer/domain/models.py:977-996 / src/multiplayer/migrations/003_frozen_artifact_provenance.sql / test | `ArtifactClaim` (models.py:977, fields incl. `version_id`, `text`) and `ClaimSource` (models.py:989, fields incl. `claim_id`, `output_id`, `evidence`) chain a synthesized Decision's claims back to the AgentOutput that produced them; `artifact_claims`/`claim_sources` tables are defined in migrations/003_frozen_artifact_provenance.sql. Traversability is asserted by `tests/e2e/test_first_class_branches.py:213-215`: `claims = await service.repos.artifacts.get_version_provenance(version.version_id)` then `assert {claim["output_id"] for claim in claims} == set(output_ids[:2])`. |
| "Actions wait for human approval before they execute" | src/multiplayer/security/capabilities.py:236-286 (`ToolSpec.requires_approval`, `decide`) / test | `ToolSpec` (capabilities.py:236) has a `requires_approval` field; `TOOLS` (capabilities.py:248-252) marks `task.create` and `artifact.write` `requires_approval=True`. Blocked-then-proceeds is asserted by `tests/security/test_model_tool_gateway.py:240` `test_a_tool_that_requires_approval_stops_at_the_reviewer`: after the model calls `artifact.write`, `result["tool_request"]["status"] == "PENDING_APPROVAL"` and `await svc.list_room_artifacts(room_id) == []`, then after `await svc.approve_action(pending[0].approval_id, "owner", require_member=True)`, `artifacts = await svc.list_room_artifacts(room_id)` is non-empty. |
| "What an agent may do is re-read from the room's own state at the moment it acts, so leaving a room or losing access takes effect immediately, mid-task" | src/multiplayer/services/service.py:3668-3669 (re-derives `terms.effective` inside `execute_agent_step`, not cached from run start) / test | Inside `execute_agent_step` (service.py, the function containing lines 3630-3752), `authorization = await self._authorization_for(...)` and `terms = await self._authorized_terms(authorization)` are recomputed on every call, i.e. every step/tool call re-reads room membership/capabilities rather than reusing a value cached when the run started. Mid-task revocation taking effect on the very next action is asserted by `tests/security/test_continuation_authority.py:91` `test_a_grant_withdrawn_between_two_tool_calls_stops_the_second`: it calls `svc.set_member_capabilities(room_id, "owner", [], "owner")` between two tool calls inside one turn, then asserts the second call never reached the model (`len(provider.prompts) == 1`, line 122), the run settled with `execution.status is ExecutionStatus.FAILED` and `"no effective capability" in execution.error`, and `run["settlement"] == RunSettlement.AUTHORITY_REVOKED.value`. |
| "Every room's event log is hash-chained... tamper-evident by construction, checkable with the audit CLI" | src/multiplayer/migrations/033_the_log_commits_to_its_past.sql / src/multiplayer/security/audit.py:22,65 / src/multiplayer/manage.py:11,97,133-134 / test | Migration 033 adds `room_events.prev_hash` and `event_hash` columns, chaining each event to the one before it. `event_chain_hash` (audit.py:22) computes the digest; `verify_event_chain` (audit.py:65) walks the chain. `manage.py` docstring documents `python -m multiplayer.manage app.db audit verify`; the `audit`/`verify` subcommand is wired at lines 97 and 133-134. Tamper detection is asserted by `tests/security/test_event_chain.py:71` `test_an_edited_payload_breaks_the_chain_at_its_row`, which runs `UPDATE room_events SET payload = ? WHERE room_id = 'room_1' AND sequence = 2` then asserts `verify_event_chain` reports a break at that row. |
| "One Python process and a SQLite file, self-hosted" | pyproject.toml:44-45 (`[project.scripts]` → `xyzzy = "multiplayer.server:main"`) / src/multiplayer/server.py:464 / src/multiplayer/db/connection.py:1,14,46 | `server.py:464` `def main()` builds one FastAPI app (`create_app`) and runs it under `uvicorn` in a single process — no separate worker/broker process is started. `db/connection.py:1` docstring: "Database connection and query helpers using aiosqlite."; `db/connection.py:14` `import aiosqlite`; `db/connection.py:46` `self._db = await aiosqlite.connect(self._path, isolation_level=None)` — a single SQLite file, no external database service. |
| "Point specialists at Ollama, LM Studio, or any OpenAI-compatible server instead of a hosted API" | src/multiplayer/model_providers/openai_responses.py:315-322 | `model_provider_from_environment` reads `base_url = values.get("XYZZY_LOCAL_MODEL_BASE_URL", "").strip()`; when set, it builds `OpenAIChatCompletionsProvider(base_url=base_url, model=model, api_key=api_key, timeout_seconds=timeout)`, i.e. any base URL implementing the OpenAI chat-completions API, which Ollama and LM Studio both expose. docker-compose.yml:15 shows a commented example: `XYZZY_LOCAL_MODEL_BASE_URL: "http://host.docker.internal:11434/v1"` (Ollama's default port). |
| "Apache-2.0 licensed" | LICENSE:2-3 / pyproject.toml:11 | `LICENSE` line 2: "Apache License", line 3: "Version 2.0, January 2004". `pyproject.toml:11`: `license = { text = "Apache-2.0" }`. |
| "979 tests" | command run this session | `.venv/Scripts/python.exe -m pytest --collect-only -q` (the exact command specified) was run from the repo root; its output ends with a warnings summary and no numeric collection total is printed at that verbosity (last line: `-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html`). Re-running with the project's default `-q` addopts overridden (`.venv/Scripts/python.exe -m pytest --collect-only -o addopts=""`) prints the true total: last line `======================== 979 tests collected in 1.29s =========================`. The page shows 979, the count collected this session; the previous page said 857. |
| "Ruff and strict mypy in CI" | pyproject.toml:65-67 / .github/workflows/ci.yml:20-22 | `pyproject.toml:65-67`: `[tool.mypy]` / `python_version = "3.11"` / `strict = true` / `files = ["src"]`. `.github/workflows/ci.yml` lines 20-22: `- run: ruff check .`, `- run: ruff format --check .`, `- run: mypy src`. |
| `docker run -p 8000:8000 -e XYZZY_DEMO=1 ghcr.io/project-nexus-yr/xyzzy` opens a seeded workspace at localhost:8000 with one-click sign-in | Dockerfile:23,26,28 / src/multiplayer/server.py:84-86,249-259,290 / .github/workflows/docker-publish.yml:1,9,22,28 | `Dockerfile:23` `EXPOSE 8000`; `:28` `CMD ["python", "-m", "multiplayer.server", "/data/multiplayer.db"]`. `server.py:84-86` `_demo_mode_requested()` reads `XYZZY_DEMO` from the environment. `server.py:259` `auth_tokens = {DEMO_BEARER_TOKEN: DEMO_USER_ID}` (a fixed bearer token — the "one click" sign-in) and `server.py:290` `await svc.seed_demo_workspace()` seeds the demo workspace. `.github/workflows/docker-publish.yml:1` (`name: image`), comment at line 9 references `docker run ... ghcr.io/project-nexus-yr/xyzzy`, `:22` `registry: ghcr.io`, `:28` `images: ghcr.io/project-nexus-yr/xyzzy`. The `docker run` command itself was **not run this session** (task explicitly forbids running docker). |
| `docker compose run --rm --service-ports demo` exists | docker-compose.yml | The `demo` service is defined with `XYZZY_DEMO: "1"`; the ops track changed the invocation from a profile-gated `up` to `run --rm --service-ports` to resolve a port clash with the `xyzzy` service, which is now profile-free. Not run this session (docker forbidden by task). |
| "Without an API key, runs produce a labelled SIMULATED output" (implied by the "self-hosted" / no-hosted-API framing on lines 449, 452; the actual fallback behaviour) | src/multiplayer/model_providers/openai_responses.py:71,326-328 / test | `openai_responses.py:326-328`: `api_key = values.get("OPENAI_API_KEY", "").strip()` then `if not api_key:` then `return WorkflowOnlyModelProvider()`. `openai_responses.py:71`: the fallback content string starts `"SIMULATED WORKFLOW OUTPUT — no model provider is configured. "`. Asserted by `tests/regression/test_agent_outputs.py:60`: `assert output.content.startswith("SIMULATED WORKFLOW OUTPUT")`, and by `tests/security/test_agent_template_ownership.py:208` (same assertion, explicitly run "offline against the SIMULATED provider" per the comment at line 189). |
| "xyzzy 0.4.0" in the footer and "v0.4.0" in the hero proof line | pyproject.toml:7 / site/index.html (grep) | `pyproject.toml:7`: `version = "0.4.0"`. The page footer reads `xyzzy 0.4.0 · Apache-2.0` and the hero proof line `v0.4.0`, both from that manifest line. |
| SECURITY.md exists (footer link "Security policy") | SECURITY.md:1,3 | `SECURITY.md:1`: `# Security Policy`; `SECURITY.md:3`: `## Supported Versions`. |
| "Only what a person included reaches the brief" and the legend "excluded, kept in the log, absent from the brief" | tests/e2e/test_first_class_branches.py:191-215 | Three outputs are recorded (`len(...list_by_branch(...)) == 3`, line 213), two are INCLUDED and one EXCLUDED (lines 191-196), and the published version's provenance contains exactly the two included output ids (`== set(output_ids[:2])`, line 215). The excluded output stays in `agent_outputs` and is absent from the claims. |
| "The seeded demo workspace, as the one command below opens it" (hero screenshot caption) | scripts/capture_hero.py / src/multiplayer/server.py:290 | The eight `site/assets/screenshot-hero-*.png` files were captured this session by `scripts/capture_hero.py` against the demo server (`XYZZY_DEMO=1 XYZZY_PORT=8010 python -m multiplayer.server`), whose seed is `seed_demo_workspace()` at server.py:290; the docker command runs the same server with the same flag. |
| "Two or three specialist agents run at once on the same question" (loop step 02) | src/multiplayer/services/service.py:2527-2548 | `start_branch` requires `2 <= len(agent_ids) <= 3` for `BranchMode.PARALLEL`, else `DomainError("parallel mode requires two or three agents")` (service.py:2548); three run in `tests/e2e/test_first_class_branches.py:168`. |
| "Nothing is synthesized until every output has one" (loop step 03) | src/multiplayer/services/service.py:4240-4271 / positive path tested | `synthesize_branch` raises `DomainError("every branch output must be included or excluded")` at service.py:4271 when any output lacks a disposition; the positive path is `tests/e2e/test_first_class_branches.py:191-198`. The refusal itself has no asserting test (see the row above). |
| The floating branch card and its tag "cropped from the capture above" | scripts/capture_hero.py `crop_branch_card` | `site/assets/branch-card-{light,dark}.png` and `@2x` are cropped by the capture script from `screenshot-hero-{theme}@2x.png` at `BRANCH_CARD_BOX_2X`, so the card is the same pixels as the workspace frame behind it. |
| The three tabbed views: "General, the shared room", "the branch, two specialists, a verdict each", "the Decision Brief, every claim with its source id" | scripts/capture_hero.py `capture_views` / src/multiplayer/server.py:290 | `view-branch-*.png` and `view-brief-*.png` are captured by the producer from the seeded demo (`seed_demo_workspace()`, server.py:290) by clicking `button.branch-nav` and `#nav-artifacts`; the branch view shows Include and Exclude per output and "2 included · 0 excluded · 0 unreviewed" (see the capture), the brief shows two claims each with a `Sources: out_…` id (see the capture). |
| "No API key was set, so the specialist text reads SIMULATED" (stage caption) | src/multiplayer/model_providers/openai_responses.py:71,323-325 | Same evidence as the SIMULATED row above; the captured views show the literal SIMULATED text. |

## Slop pass

The one move a competitor could not copy without looking like a clone: **the stage**, three real captures of one seeded room (channel, branch view, Decision Brief) as tabs over a perspective stack on the deep green band, the chosen view in front and the others receding, with the room's own branch card floating in front of it; and below it the loop that draws itself a stage per step scrolled. The page's motion is the mechanism and its layers are the product's own screens. Colour: paper and deep forest green in alternating bands, gold for the one action, the tabs and the one italic word; green is the brand and the included mark, red the excluded mark. Kept by the user's call on 2026-09-02: the palette and the Cormorant display with the italic gold word, which the slop tells list as a reflex and which is here a decision. Reflexes removed: the gold full stop in the wordmark, the three-column feature grid (now a ledger with proof), the dash as sentence punctuation, a test count carried over from an earlier run, any gradient beyond one gold light on the stage, ambient looping motion, parallax on decorative shapes, and any screen that does not exist. Kept on purpose: the clone URL wrapping at 390 px rather than hiding behind a scrollbar, and the SIMULATED text visible in the captures rather than a retouched one.
